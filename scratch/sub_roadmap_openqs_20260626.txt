# Subagent: Roadmap Open Questions Review & Consolidation (High-Value Cursor Handoff)
**Date:** 2026-06-26
**Task:** Read-only review of current open questions in docs/design/roadmap-and-open-questions.md, cross-ref with recent deepens in architecture-overview.md (LW ReplayController) and GDD.md (voice psych), game-mechanics-spec.md, RESEARCH_STATUS.md (subs voice/LW/anti-cheat), and cursor-tasks. Extract, prioritize, and propose concise updates or consolidated list to add to roadmap (or STATUS) for better handoff. Focus high-value for Cursor agents. Output structured findings + suggested append text. Save full here.

**Scope (read-only):** Used list_dir, read_file (full + chunks/offsets), grep (patterns for open/question/TODO/risk + specific terms like ReplayController/LW/voice/anti-cheat/determinism). No edits. Cross-checked all specified files + supporting (IMPLEMENTATION_HANDOFF.md, anti-cheat-determinism-design-notes.md, TASK_MANIFEST.md, sample tasks 001/005/007/008, scratch subs like sub_lw_controller_20260626.txt / sub_webrtc_voice_research_20260626.txt, verif_*.txt).

## Structured Findings

### 1. Current State in roadmap-and-open-questions.md
- **Phased Roadmap**: Phases 0-5 mapped to cursor-tasks/TASK_MANIFEST.md (8 canonical tasks). Phase 4 bundles Voice + Anti-Cheat Hardening + Replays. Recent updates note (lines ~122-127): Architecture deepened w/ LW ReplayController (pseudocode/logical sync/overlays/deltas/Mermaid/React/pitfalls; see arch §5.1 from LW subs; boosts task-007). GDD §8 enriched w/ FXR "mental warfare" quotes + sabo/TB bluff exs + VoicePanel/WebRTC (from voice sub). Handoff sustains All hold (8/8, 35+ TASK_MANIFEST refs, 0 legacy). "Roadmap open Qs remain prioritized (anti-cheat, desync, voice NAT, etc.); recent research addresses some (determinism in arch notes, psych in GDD)." Explicitly notes "Background sub spawned for open Qs consolidation/prioritize with latest." (This sub.)
- **Open Questions & Risks (Consolidated)**:
  **High Priority (resolve before/during Phase 1-2)**:
  1. Clock & Sync Details: Preferred internal tick rate vs wall time? Broadcast freq (fixed vs event-driven)? Client-side prediction for own actions (orders, TB) or pure server-follow?
  2. Data / Arena Details: Exact storage (full OHLC vs returns-only + on-demand indicators)? Arena selection algo (random balanced, MMR, manual)? Volume/extra fields in normalized data?
  3. Voice: WebRTC self-hosted (STUN/TURN) vs provider (Twilio/Agora)? Priority: in-app vs Discord fallback? Recording/post-match voice log opt-in?
  4. Replays & Persistence: Format (event log + arena ID for exact re-sim vs full state snapshots)? Storage (Postgres, files, object store)?
  5. Anti-Cheat & Fairness Validation: How to prove determinism across runs/players? Testing strategy for lag sim on clock contention? Client reconnect mid-match catch-up (full snapshot + missed deltas)?
  **Medium/Later**: Exact order fidelity (close-only vs high/low triggers within bar), IP economy tuning (regen/P&L %/cap), sabotage visibility/counters (e.g. "purge"?), UI chart efficiency (deltas vs windowed), scaling edges (room migration/spectator/high concurrency), stack constraints (Python?), future intraday/options/real-money (legal).
  **Risks & Pitfalls** (from arch): Desync (mitigate det sim + periodic snapshots + reconnect); Voice NAT/latency (fallback provider ready; test early); Data staleness/gaps (robust cleaning); Cheating via external knowledge (norm + private arenas + generic labels primary; + mystery slice); Python GIL (fine MVP 1v1; async/multi-proc per room); Over-eng (in-mem MVP; Redis only when needed); UX of contested time (make TB very visible/satisfying; tune costs strategic not frustrating).
- **Alignment**: Points to TASK_MANIFEST + design package (GDD/mech/arch) as complete for agents. "Next Steps: ... Start per ... Use Cursor Agent on one task at a time with full spec context open."

### 2. Cross-Refs from Recent Deepens + Supporting Files
- **architecture-overview.md (LW ReplayController deepens, §5 Frontend, added ~2026-06-26 15m review per LW sub 019f058c + official tutorials)**:
  - **Deepened Content**: "Preload norm data (setData init); update() for deltas/stream (server T or R-scaled timer). Sync: timeScale visibleLogicalRange subs for multi-symbol/scrub/FF. Overlays: markers for trades, priceLines for levels, parallel series for equity. React: refs for live updates from WS/timer. ... Suggested: ReplayController + Mermaid in full docs."
  - **LW Replay Controller Details**: ReplayController (custom hook): React useEffect + setInterval/requestAnimationFrame scaled by R. Ex: `const interval = setInterval(() => { chart.timeScale().setVisibleLogicalRange(aroundCurrentT); series.update(newBar); rebuildMarkersAndPriceLines(); }, baseMs / R);`. On pause (R=0): clear/freeze. On FF/scrub: adjust/set range directly. Server drives auth T/R via deltas; client follows + smooths.
    - Logical Range Sync: `timeScale.subscribeVisibleLogicalRangeChange(range => { slaveChart.timeScale().setVisibleLogicalRange(range); });` (master/slave avoid loops). Fractional OK for precise scrub/FF around T index. fitContent(), setVisibleLogicalRange({from,to}).
    - Overlays: Markers (createSeriesMarkers or setMarkers; full replace on changes; {time, position, shape, color, text}). PriceLines (series.createPriceLine({...}); applyOptions/remove). Equity: parallel Line/Area series (same or sep priceScaleId; update({time, value})).
    - Delta Types (from task-005): BarDelta, ResourceDelta {T,R,TB_level,...}, OrderDelta (list/diff -> markers/priceLines), EquityDelta, EventDelta (fills/sabo notifs/news).
    - React/TS: useRef<IChartApi>, useRef<ISeriesApi>; useEffect create/add/setData/ subs / cleanup. WS onmessage: if(refs) {series.update(bar); updateOverlaysFromState();}. forwardRef/Context for <SyncedChart>.
    - Mermaid (timer/WS flow): Server -> WS deltas -> Client -> ReplayCtrl (scaled int by R) -> ChartUpdate (update + setVisibleLogicalRange) + Overlays.
    - Pitfalls (confirmed): update() only for latest (setData for inserts); master/slave sync; full marker replace; ref leaks (strict cleanup); no extrapolation on ranges; server T truth + conservative client.
    - Handoff: Full in scratch/sub_lw_controller_20260626.txt + RESEARCH_STATUS + official https://tradingview.github.io/lightweight-charts/tutorials/demos/realtime-updates + /tutorials/react/advanced. Follow for task-007.
  - **Open Qs noted in LW subsection**: Client scrub sends action? Smoothing lerp for R? Current-T indicator plugin/marker? Equity separate pane?
  - **Broader §9 Potential Pitfalls & Open Tech Questions** (10 items, prioritize before impl; overlaps roadmap):
    1. Exact internal sim tick rate vs. broadcast rate? (e.g. 60Hz sim, 10Hz net?)
    2. Client-side prediction/optimism level for orders and tempo (to hide latency)?
    3. Data format for arena slices (full OHLC arrays vs. returns-only + base price)? Volume handling?
    4. Voice signaling details + TURN server choice (self-hosted vs. provider)?
    5. Replay format: full event log + arena vs. video export vs. state snapshots for scrubber?
    6. Multi-server state sync strategy (Redis JSON vs. dedicated game servers)?
    7. Edge cases for shared R contention (simultaneous Pause + FF; fractional T triggers)?
    8. How to handle reconnects mid-match (catch-up snapshot + catch missed bars)?
    9. Intraday bars viability and impact on engine (higher fidelity vs. perf)?
    10. Monetization hooks (e.g., cosmetic abilities) — any backend implications early?
  - Pitfalls list: Clock/Desync (server T auth + smoothing/interp; test lag), Determinism (FP/order ops/non-trading; identical codepaths + tests), Bandwidth (deltas vs full; LW handles streaming but tune), Voice NAT/Quality (P2P fails strict firewalls -> TURN; test cross-net), Resource Contention UI (clear "who controls R"), Cheat vectors, Replay bloat, Scalability trap (in-mem -> Redis), Balance vs Tech (sabo costs vs sim fidelity), Legal/Hosting.
  - Voice section (§5): WebRTC P2P rec for 1v1 (signaling via WS; getUserMedia + RTCPeerConnection; STUN/TURN; PTT/mute/volume per player). 3rd-party fallback (Twilio/Agora etc. for reliability). Text-first MVP. <150ms target. Opt-in post-match logs. Hybrid/Discord possible. Reqs per spec.
  - Anti-cheat (§6): Norm + private arenas primary; auth server; data isolation; visibility (sabo always notify); rate limits/validation; replay analysis; det sim + seeded RNG; server news.
  - Realtime sync (§2): Authoritative server + WS; shared clock server-owned (T/R via TB rules); deltas ~10-20Hz or events; desync mitigate det + snapshots + reconnect; rooms per-match.

- **GDD.md (voice psych enrich, §8 Social & Psychology Layer + research insights)**:
  - Core: Voice + text match chat persistent. Trash talk, bluffing, reactions to moves. Huge from FXR Battles research: quotes "Trading is more than charts, it’s mental warfare... Live voice chat to talk strategy or trash. Text chat for reactions, quick calls, and taunts." "Yes — live voice and text chat during active battles."
  - Psych examples (from voice sub): Bluff/deny during sabotage ("Nice SLs... or were they?"); taunt TB control ("I'm holding pause — you're wasting IP"); react to peeks/fills/equity swings. Poker-like tells + pressure. Pair with visual indicators (speaking during sabo toasts).
  - Integration: VoicePanel next to text chat (task-007/008); enhance text w/ emoji reactions to events; opt-in controls, PTT/mute. WebRTC P2P + WS signaling (extend task-005 rooms) or fallback. <150ms target. Graceful degrade + NAT testing. Benefits: engagement, psych edge, fun factor. Risks: toxicity (mitigate mute/volume/PTT). See RESEARCH_STATUS voice sub, arch §5, task-008.
  - Research insights (15m review): Integrate LW Charts for replay (update streaming, sync, overlays from sub); voice as psych differentiator (FXR mental warfare, trash talk validated); sabotage/TB for MOBA contest on historical. See STATUS subs, arch for details. Polish UI for contested feel (TB controls, IP actions, synced charts).
  - Other refs: Voice in vision §1, tech §11, opens §12 (voice chat impl priority vs text?); roadmap Phase 4.

- **game-mechanics-spec.md (detailed ambiguities §12 + mechanics grounding opens)**:
  - **Ambiguities & Open Questions** (to resolve before/full GDD or impl; highest priority listed; see thinking trace + GDD for full):
    1. Exact shared-clock contention edge cases and UI feedback. Personal-view alternative?
    2. Precise tuning (all rates, costs, durations, spread %).
    3. IP generation formula and economy balance.
    4. Sabotage visibility granularity and counters.
    5. Arena selection, anonymization strength, and data-prep pipeline details.
    6. Order model fidelity (closes only vs. high/low triggers).
    7. Voice priority and fallback.
    8. Tiebreakers, surrender, and post-match reveal policy.
  - Detailed grounding: Time model (shared T/R; 1s real=1 day; 300 bars base; non-trading skip/interp; visual generic "Sim Day"). TB (0-100; recharge +1/s base +4/s pause bonus; costs 0/2/5/12 per real-s for Pause/FFx2/3/5; contention: Pause overrides R=0; max FF payers consume; visual "Market Speed"). IP (start 50; regen +0.5/s +10% realized P&L; for feeds/peek/sabo; not tempo). Orders (Long/Short; Market/Limit/Stop+attached SL/TP; exec vs bar at T; P&L realized/unrealized; base spread; no complex margin MVP). Feeds (base free 5 instr + MA; IP one-time unlocks more/techs/macro). News/peek (60 IP, 1/match or cd; real headline at T). Sabo (IP cost, cd 45-90s; always notify victim w/ timestamp; 3 effects: delete SLs 30IP/60s; widen spreads 25IP/45s/15s; fake news 40IP/90s). Win (higher final equity after 5min real; ties secondary). Chat/voice (§9: text always; voice core per vision; WebRTC/equiv, PTT+mute+vol; <150ms; reliable join; easy mute; text fallback; opt-in post logs; fun: bluffs/reactions/pressure/"all-chat" MOBA salt). Normalization (§10: P[0]=100 all; % returns only; generic labels during; private server arenas; real optional post). MOBA flavor throughout.
  - Open in sections: e.g. Time: max R? fractional T partial-bar interp? Resources: IP from unrealized/neg? burst FF? Sabo: specific instr? purge counters? probabilistic detect? Order: notional sizing? simultaneous limit priority? high/low vs close-only? Info: recurring vs one-time IP? lost via sabo? Num charts. Peek: past days? partial/full? fake verify? Win: sudden-death? best-of?
  - Anti-cheat/fairness: identical slice + private + % norm + generic; server sim; rate limits; mystery offsets/variants.
  - Scope: MVP basic orders+3 sabo+peek+tempo+feeds+chat. No derivs/real$ .

- **RESEARCH_STATUS.md (subs voice/LW/anti-cheat + process log + carried opens)**:
  - **Overview**: 4h autonomous research/design run (yolo/always-approve). All core complete: GDD deepened, Mech Spec, Arch, Roadmap/Handoff. Cursor handoff prep via TASK_MANIFEST (8 tasks). Subs + schedulers (2/5/10/15m) + background for momentum. Verifs: 8/8 tasks, 30+ TASK_MANIFEST refs (STATUS/plan/arch/roadmap/GDD/cursor-README/handoff), 0 legacy inline "**Cursor Tasks**:" lists in design/, clean handoff.
  - **Key Subs Integrated (voice/LW/anti-cheat)**:
    - **LW Charts Deep (019f0577 + 019f058c controller sub)**: Targeted for task-007 + arch. Streaming: preload setData(initial norm); update(bar) for deltas/appends (NOT setData for updates - perf warning per official). Multi-sync: timeScale subscribeVisibleLogicalRangeChange + setVisibleLogicalRange (master/slave). Overlays: markers (createSeriesMarkers/setMarkers for entries/exits/trades), priceLines (create for SL/TP/levels), equity parallel series. React/TS: useRef + useEffect (create/add/setData/resize/cleanup; strict); WS onmessage drive updates; advanced forwardRef/Context composables. ReplayController: custom (useEffect/setInterval/raf scaled by R: baseMs/R; tick -> update + setVisibleLogicalRange around T; on pause clear; FF/scrub direct range). Server T/R auth; client smooth/follow. Deltas from 005. Time formats consistent (YYYY-MM-DD or ts). Perf: ~300 bars trivial. Pitfalls: update only latest; insert middle needs rebuild/setData; sync loops (master/slave); marker full-replace; ref leaks; visible jumps; no range extrapolation; time consistency. Sources: official realtime-updates tutorial (exact setData+update+setInterval+scrollToRealTime exs), time-scale docs, series, how_to/series-markers/price-line, advanced React tutorial; GitHub plugin-examples; tradingview.com. Recs: LW v5+; preload norm; server drive clock; equity+markers/priceLines; timer+logical for replay; follow for task-007. Expanded arch w/ controller + Mermaid. High value handoff. See scratch/sub_lw_controller_20260626.txt + web transcripts.
    - **Voice / WebRTC + Psych (multiple incl. 019f058c-b2e9... + 019f057c)**: High readiness for task-008 (self-contained, strong alignment to specs/vision/research/FXR "mental warfare"/psych layer; ready as-is for Cursor). Tech: WS signaling reuse (extend task-005 rooms: offer/answer/ICE trickle targeted to 1v1 partner; server dumb relay). Native P2P: RTCPeerConnection + getUserMedia (audio constraints echo/noise/gain). iceServers: STUN (Google) + TURN (Coturn ex). <150ms target (Opus low overhead; monitor getStats RTT/jitter). Graceful: connectionstate 'failed' -> TURN or 3rd-party (Twilio/Agora/LiveKit) or text toast. Perms: getUserMedia err (NotAllowed -> text-only + re-request); Permissions API. Indicators: Web Audio AnalyserNode + RMS for speaking (local/remote pulsing/volume). NAT: ~20-25% need relay (symmetric/CGNAT/firewalls); mitigations multi STUN/TURN geo, trickle, cross-net test early. Matches arch pitfall. Psych/mental warfare (high value for GDD/mech/task-008): FXR validation exact quotes above. Examples tied to gameplay: sabotage (bluff "Nice SLs... or were they?", deny, tilt); TB ("I'm holding pause—you're wasting IP"); peeks ("I peeked the headline"). Benefits: engagement, psych edge, fun. Risks: toxicity (PTT/mute/volume). Integration: VoicePanel next to TextChat (task-007); Zustand + WS events; enhance text w/ emoji reactions to sabo/TB; clear contested feedback. Recs: 1. signaling over 005, 2. native P2P, 3. controls/indicators, 4. fallback, 5. text/polish/perms, 6. tests. Emphasize psych UX + sabotage pairing. See scratch/sub_webrtc_voice_research_20260626.txt + STATUS full excerpts/FXR blocks + web. Gaps (inherited): WebRTC vs provider (self STUN/TURN e.g. Coturn vs Twilio/Agora/LiveKit; in-app vs Discord open in roadmap); NAT specifics; exact libs/TURN config/audio; fallback seamlessness; post-match recording (non-goal MVP per task); UI details (voice join events). Task/ arch/roadmap leave choice open.
    - **Anti-cheat (019f056b + prior; notes in anti-cheat-determinism-design-notes.md)**: High value for task-001/005. Detailed: action+input logging + exact re-sim (clients actions only; server timestamp/log/replay arena+seq for verify); GGPO/Photon/Bevy patterns (rollback, inputs-only, checksums, synctest); tailored pseudocode/harness (normalized P[0]=100 private arenas, TB resolver, server sim, logging). Citations GGPO/Photon. See STATUS process log + design/anti-cheat-determinism-design-notes.md (full expanded: 1. logging/re-sim pseudocode for verify_replay + state_hash; 2. hashing/checksums arena+actions+periodic (Decimal for floats); 3. shared state contention (TB resolver _recompute_R w/ Pause override/max FF; latency handling optimistic+lerp); 4. private arenas + norm; + more on desync detection, lag sim tests, replay scrubber from logs, harness ideas for task-001/005). Excellent for Cursor det tests (contention, fills, sabo+notifs, replay hash/identical).
    - **Other Task Subs (004/005/007/008 reviews)**: High readiness overall (self-contained Goal/Reqs/ACs 1-7 + full Cursor Prompt + Integration Notes + refs to specs/prior tasks/handoff/roadmap; strong alignment; ready as-is paste + full design pkg). Minor gaps (mostly carried spec opens): e.g. for 005 realtime: spec opens (max R, fractional T/partial interp, IP unrealized/neg, burst FF, sizing, simultaneous fill prio, bar fidelity close-only vs H/L, sabo granularity); 004/005/002 overlap (need shared clean interfaces for order exec/state/clock); arch/roadmap opens (tick/broadcast rate ~10-20Hz vs 10-60Hz; client pred/optimism "no heavy" vs conservative buffer/lerp/snap; reconnect full catch-up vs basic snapshot; shared R edges simul Pause+FF/timing races; arena OHLC vs returns). Similar for 007 (carried broadcast/tick, client interp/smoothing var R/FF/pause/scrub, TB "who influencing" visibility anon option, feed UI gran, bar fidelity visuals, fractional T display; 007/005 overlap clean shared WS deltas/types; lobby stub not core AC). For 008: as voice sub above. Recs: proceed as-is; agent strict spec nums (bar-at-T + units defaults), reusable models (SharedClock/TempoBarResolver etc), det tests (contention, fills, sabo notifs, replay hash), basic reconnect/snapshots, wall-time base + ~10-20Hz or on change; clean data/arena IFs; server truth/contested; emphasize psych/trash in UX. Pre-handoff optional: add structure notes/tighter arch ref. Package high readiness.
  - **Process Log Details**: Fresh web/open_page (FXR/ChartChamps/Hedgd full excerpts validating voice psych + historical fairness); LW/WebRTC/analogs research; multiple self-checks/scheduler/background subs (voice/anti-cheat/LW/task reviews); verifs (lists/greps/reads/PASS All hold); Cursor opens (tasks + design); patches/scratch evidence. "Highest-value remaining" identified as deepen arch (LW controller) + GDD psych (done); or roadmap. "Roadmap open Qs current (anti-cheat determinism, desync, voice, scaling)." Many "carried opens" noted in reviews. "All hold sustained." Background sub for open Qs explicitly spawned in 10m self-check.
  - **Research Summary**: Data (yfinance/Stooq primary; Parquet; norm P[0]=100), analogs (ChartChamps 1v1 historical Elo/replays; FXR voice mental warfare + historical; Hedgd duels/scrub; etc. Diff: TB/IP/sabo + private norm arenas + contested shared clock), mech (as spec), tech (FastAPI WS auth, LW streaming, WebRTC, det sim, anti-cheat logs/hashes/norm).
  - **How to Resume**: Check this + design files + todos + cursor-tasks/ + scratch/.

- **cursor-tasks/ (TASK_MANIFEST + samples; high-value for agents)**:
  - **TASK_MANIFEST.md**: Canonical table exactly 8 tasks (001 anti-cheat-normalization.md, 002 time-and-tempo, 003 resources-sabotage, 004 orders-core-loop, 005 realtime-backend, 006 data-pipeline, 007 frontend-streaming, 008 voice-integration). Notes: Use this for inventory; all self-contained; ref design/; history (001 separated from data for clarity; 006 primary data).
  - **README.md**: How Grok/Cursor agents: open design + task in Cursor, `cursor agent`; helper script launch-cursor-agent.ps1; tips (full task as instr; .cursorrules). Current: see MANIFEST.
  - **Sample Tasks** (self-contained format: Context/refs, Goal, Requirements (from specs+arch), Non-goals/MVP Scope, Acceptance Criteria (specific numbered), Cursor Agent Prompt (copy-paste ready, refs specs), Integration Notes, Priority/Size/Owner):
    - task-001-anti-cheat-normalization.md: High prio core to fairness/MVP. Req: P[0]=100 % returns; generic labels; private server arenas; det (same arena+seq identical state/P&L); verify harness/replay log; mystery offsets. ACs 1-6: norm func, arena loader, det test (bit-identical), logging, sim integ, docs. Prompt refs roadmap too. Integ: w/ 006 data, 005 sim, FE. (No code yet.)
    - task-005-realtime-backend.md: High prio core. Req: server owns SharedClock/TB resolver (exact rates/contention from mech-spec), order exec/P&L, IP/sabo effects (notify victims); clients actions only; WS deltas ~10-20Hz/changes (T/R/bars/equity/orders/resources/events); in-mem rooms; det; wall align; no heavy client pred. ACs 1-7: FastAPI WS rooms, SharedClock+TB exact, sim loop, action models, basic reconnect/snap, tests (det/contention/fills/sabo+notifs), clean IFs. Prompt refs arch/mech. Integ: w/ 006/002/003/004/001; preps 008 signaling/007. 
    - task-007-frontend-streaming.md: High prio playable. Req: React/TS + LW (or equiv) multi-instr synced charts realtime series.update() from WS deltas; panels (charts shared T, orders w/ market/limit/stop+SL/TP, TB controls Pause/FF show R/influence, IP spend feeds/sabo, notifs, text chat); send actions; norm display P[0]=100; basic lobby stub; duel feel. ACs 1-8: WS hook, live charts, order panel, TB controls, IP UI, notif/chat, tests/stories, "fast tense personal duel". Prompt refs arch/mech/GDD. Integ: w/ 005 backend; 008 voice; 006 data.
    - task-008-voice-integration.md: High for fun layer. Req: signaling over 005 WS; WebRTC P2P or 3rd fallback; UI controls mute/PTT/vol; fallback if fail; text polish timestamps/emoji reactions/scroll; mic prompt; visual speaking indicators. MVP 1v1; text-first; no recording. ACs 1-7: WebRTC+WS sig, audio+controls, fallback, VoicePanel next text chat, text enh, tests conn/fallback, graceful mic deny. Prompt: "Follow 'psychological warfare' and 'trash talk' emphasis from vision." Integ: 005 WS, 007 UI; huge diff per FXR.
  - Other tasks (002/003/004/006 per manifest): Similar self-contained; 002 time/tempo core (exact TB from spec); etc. Agents instructed "Start by reading the referenced specs and manifest." "Ask only for hard blockers." Strict follow specs (no invention on opens).
  - **Handoff Value**: Tasks designed for Cursor (full prompt + ACs + refs; agents get opened context + paste task). High readiness per subs (self-contained; strong spec align; minor carried opens/overlap). Cursor README + launch script for autonomous. All 8 present, canonical via manifest.

- **Supporting (IMPLEMENTATION_HANDOFF.md, anti-cheat-notes, verifs/scratch)**: Handoff package points to design pkg + MANIFEST; updates note subs/LW/voice/All hold. Anti-cheat-notes.md: actionable pseudocode (SimulationEngine, TempoBarResolver, verify_replay w/ hash, periodic checksums w/ Decimal, action logging), patterns (GGPO/Photon inputs-only re-sim/referee, Bevy synctest checksums), recs for 001/005 (persist log+arena+score; auto verify; harness 2 virtual players + fuzz + lag sim). Scratch: verif_* (8/8 PASS, counts, greps, 'All hold' proofs), sub_*.txt (LW controller full ~patterns/excerpts; webrtc voice full; task007 review high readiness), patches. plan.md: verif steps.

### 3. Prioritization (High-Value for Cursor Agents)
Focus: Impact early phases (0-3: 001-007), affect data models/interfaces/tests/det/UX in core tasks, actionable w/ recent deepens (LW equips 007 replay w/o invention; voice psych equips 008 UX/bluffs; anti-cheat equips 001/005 harnesses). Agents benefit from concrete "resolve before" + "MVP defaults per spec" + "test these" + "refs for details".
- **Tier 1 / Critical (Phase 0-2; 001 anti-cheat, 002 time, 005 realtime, 006 data; block det/sim/anti-cheat/fairness)**: 
  - Determinism proof/harness + state hashes/checksums + re-sim verify (anti-cheat sub pseudocode; roadmap #5; arch pitfalls; mech; STATUS/anti-notes). Test: identical outcomes across "players"/runs; lag sim contention; replay hash.
  - Shared clock/TB contention edge cases + UI feedback (roadmap #1; arch #7; mech #1; STATUS carried). Incl: simul Pause+FF; fractional T triggers/partial interp; Pause override/max FF payer consume; who-influencing visibility (anon option?); optimistic client + lerp/snap to server R.
  - Data/arena format + pipeline details (roadmap #2; arch #3; mech #5; STATUS). OHLC arrays vs returns-only + base price/volume; arena selection (random/curated/balanced); anonymization strength (generic labels + mystery offsets); pre-norm vs per-match; non-trading handling.
  - Reconnect mid-match catch-up (roadmap #5; arch #8; STATUS 005 review). Snapshot + missed deltas/bars.
- **Tier 2 / High (Phase 1-3; 003/004/005/007; affect orders/resources/FE)**:
  - Order model fidelity + fill/priority (mech #6; STATUS 004/005: close-only vs high/low triggers within bar; simultaneous limit prio; sizing model; spread %).
  - IP economy (mech #3; STATUS: formula +10% realized P&L; regen +0.5/s; from unrealized/neg?; soft cap ~200; burst instant FF?).
  - Sabotage visibility/granularity + counters (mech #4; STATUS: notify always/timestamp; specific instr? purge? probabilistic?).
  - Client prediction/optimism + smoothing (arch #2; STATUS; LW: conservative for R/FF/pause/scrub).
  - Sim tick/broadcast rate (arch #1; STATUS 005: ~10-20Hz or on change vs higher internal; wall align).
  - LW FE specifics (arch LW open Qs post-deep: client scrub sends action?; smoothing lerp for R?; Current-T indicator plugin/marker?; equity sep pane? Also: master/slave sync, overlays rebuild, ref lifecycle).
  - Arena prep/selection (overlap Tier1).
- **Tier 3 / Medium-Later (Phase 3-4+; 007/008 replays/voice; scale)**:
  - Voice (roadmap #3; arch #4; mech #7; STATUS voice sub): WebRTC self (STUN/TURN e.g. Coturn) vs provider (Twilio/Agora/LiveKit); in-app vs Discord fallback; signaling details; NAT test/cross-net early; recording opt-in/post logs (deferred MVP); latency <150ms; psych UX (bluffs/trash per FXR + exs); fallback seamlessness; text polish.
  - Replays/persistence (roadmap #4; arch #5): Format (full event log + arena ID for exact re-sim vs state snapshots vs video export/scrubber); storage; post-match full visibility (incl opp feeds/buys); scrubber from log (re-sim or snapshot).
  - Multi-server/scale (arch #6): Redis pub/sub vs dedicated; room migration; spectator.
  - Tuning (mech #2: all rates/costs/durations/spread %; IP cap/regen).
  - Post-match: tiebreakers/surrender/reveal policy (mech #8; roadmap).
  - Future: Intraday (arch #9; viability/perf), monetization (arch #10), stack (Python GIL?).
- **Cross-Cutting / Mitigations**: Desync (det + snapshots + good reconnect; test lag); voice NAT (fallback ready; early test); data gaps (robust scripts); cheat external (norm+private+generic primary); perf (in-mem MVP); UX contested time (visible TB/psych feedback).
- **How Recent Deepens Help**: LW equips task-007 exactly (patterns/pitfalls/sources in arch/scratch/STATUS; agents follow w/o invention). Voice psych equips task-008 UX/mental layer (quotes/exs in GDD/STATUS; pair w/ sabo/TB). Anti-cheat notes equip 001/005 (pseudocode/harness for det/logging/TB). Task reviews confirm "high readiness" but flag carried opens for agent awareness (strict spec, no invention). Roadmap/STATUS note "recent research addresses some".
- **For Cursor Agents Specifically**: 
  - Paste full task + design pkg (GDD/mech/arch w/ LW/voice) + STATUS excerpts (subs + carried) + MANIFEST + anti-notes + relevant scratch subs.
  - Agents: "Follow exact rules from specs. No invention on opens." "Define models first." "Reusable sim logic + det tests." "Update STATUS + open Qs post-impl."
  - High value next: Resolve Tier 1 early in prototypes (e.g. via task-001/002/005/006 parallel); use anti-cheat harness for verif; equip FE w/ LW controller from arch; voice psych for UX in 008/007.
  - Evidence of readiness: 8/8 tasks self-contained; verifs PASS; handoff "complete/high/All hold" in multiple files.

### 4. Gaps & Recommendations
- Roadmap open Qs somewhat static/outdated vs deepened (missing explicit LW open Qs, anti-cheat sub details, carried from task reviews, specific psych tie-ins). STATUS has most comprehensive (process log + subs), but not consolidated "add to roadmap" list. Tasks defer to specs (good, no dup).
- Suggest: Append consolidated/prioritized list (below) to roadmap-and-open-questions.md (under Open Questions or as "Updated for Cursor Handoff post-deepens"). Or to RESEARCH_STATUS.md (new section) or IMPLEMENTATION_HANDOFF.md. Keep concise, phase/task-mapped, ref sources/subs for depth. Update version/date.
- Also: Ensure future task updates or STATUS note resolutions (e.g. "Resolved in task-005: use X tick rate").
- High-value deliverable: This enables better Cursor handoff (agents see prioritized, equipped w/ recent deepens).

## Suggested Append Text (Concise; Ready to Paste)
**Copy-paste candidate for end of "Open Questions & Risks (Consolidated)" section or new subsection in roadmap-and-open-questions.md (or append to RESEARCH_STATUS.md / IMPLEMENTATION_HANDOFF.md):**

---

**Updated Open Questions for Cursor Agent Handoff (post 2026-06-26 deepens; consolidated/prioritized from roadmap + arch/GDD/mech-spec + RESEARCH_STATUS subs voice/LW/anti-cheat + task reviews)**  
**Version**: 0.2 (2026-06-26) | Refs: arch §5 (LW), §9; GDD §8; mech-spec §12; STATUS (process log + subs + carried); anti-cheat-determinism-design-notes.md; TASK_MANIFEST + tasks 001/005/007/008; scratch/sub_lw_*.txt + sub_webrtc_*.txt.  
Recent: LW ReplayController details (arch §5.1: ReplayController hook w/ scaled timer by R, logical master/slave sync, markers/priceLines/equity overlays, WS deltas, pitfalls; open Qs: client scrub action?, lerp R smoothing?, Current-T indicator?, equity sep pane?); GDD voice psych (FXR "mental warfare" quotes + sabo/TB bluff exs e.g. "Nice SLs... or were they?" "I'm holding pause—you're wasting IP"; VoicePanel + psych UX); anti-cheat sub (pseudocode/harness for logging/re-sim/hashes/TB resolver/GGPO/Photon patterns). Tasks high readiness (self-contained; agents: strict spec/MVP defaults like bar-at-T/units; no invention on opens; reusable + det tests). All hold verifs sustained (8/8 tasks, 30+ MANIFEST refs).

**Tier 1 / Critical (resolve Phase 0-2; core for 001 anti-cheat, 002 time/tempo, 005 realtime, 006 data; impact det/sim/fairness/anti-cheat)**:
1. Determinism proof + harness (anti-cheat sub pseudocode): Action logging (server ts + sim T + arena_id); exact re-sim from log + arena (verify_replay -> state_hash/equity); periodic checksums (use Decimal for FP; arena hash + key state T/R/equity/pos/orders/TB/IP); tests (bit-identical runs, 2 virtual players same seq, fuzz random actions, lag sim on contention). Persist log+arena+score. (Roadmap #5; arch pitfalls; mech; STATUS anti/LW.)
2. Shared clock/TB contention edges + UI feedback (incl fractional T, simul actions): Pause overrides R=0; R=max(FF) payers consume; who-influencing vis (anon?); client optimistic (own only) + lerp/snap to server; UI clear contested (TB/R). (Roadmap #1; arch #7; mech #1; STATUS 005/004.)
3. Data/arena format + pipeline (OHLC vs returns-only + base/volume; selection/anonymization/prep; non-trading): Private server slices; norm P[0]=100 % only; generic labels during (real post optional); mystery offsets. (Roadmap #2; arch #3; mech #5.)
4. Reconnect mid-match catch-up: Snapshot + missed deltas/bars. (Roadmap #5; arch #8; STATUS 005.)

**Tier 2 / High (Phase 1-3; 003/004/005/007; orders/resources/FE)**:
- Order fidelity + fills (close-only vs high/low triggers/bar; sim limit prio; sizing model; base spread/slippage). (mech #6; STATUS 004/005.)
- IP economy formula/balance (regen +0.5/s +10% realized P&L; from unrealized/neg?; cap ~200; burst FF lump?). (mech #3; STATUS.)
- Sabotage vis/granularity/counters (always notify timestamp; instr-specific? purge? prob detect?). (mech #4; STATUS.)
- Client pred/optimism + R smoothing (arch #2; LW conservative; STATUS).
- Sim tick vs broadcast rate (~10-20Hz or on change vs internal). (arch #1; STATUS 005.)
- LW FE specifics (from deep): Client scrub send action?; lerp for R?; Current-T indicator/marker?; equity sep pane? (Also overlays rebuild, ref clean, sync loops.) (arch LW open Qs + §5; scratch/sub_lw; STATUS; task-007.)
- Arena prep/selection (Tier 1 overlap).

**Tier 3 / Medium-Later (Phase 3-4+; 007/008 replays/voice; scale)**:
- Voice (WebRTC self-hosted STUN/TURN e.g. Coturn vs provider Twilio/Agora/LiveKit; in-app vs Discord fallback; NAT test/cross-net early; signaling WS reuse; <150ms; psych UX/trash/bluffs per FXR + exs tied to sabo/TB/peeks; fallback seamlessness; text polish + indicators; recording opt-in deferred). (Roadmap #3; arch #4 + voice §; mech #7; GDD §8; STATUS voice subs + FXR excerpts; task-008.)
- Replays/persistence (event log + arena for exact re-sim vs snapshots vs video/scrubber; storage; post full vis incl opp feeds). (Roadmap #4; arch #5; STATUS.)
- Multi-server (Redis pub/sub vs dedicated; room mig; spectator). (arch #6.)
- Precise tuning (rates/costs/durs/spreads/IP). (mech #2.)
- Post-match (tiebreakers/surrender/reveal policy). (mech #8.)
- Future: Intraday viability (arch #9); monetization hooks (arch #10); stack constraints.

**Risks/Pitfalls (reiterated + recent)**: Desync (det + snapshots + reconnect; test lag sim); Voice NAT/quality (fallback; early cross-net test); Data gaps (robust fetch/clean); External cheat (norm+private+generic primary + mystery); GIL (async/proc per room MVP); Over-eng (in-mem first); Contested time UX (visible TB/psych feedback). LW pitfalls (update() deltas only; master/slave; ref leaks; no extrap).

**MVP Defaults per Specs (for agents)**: Bar-at-T exec + units (100 start); exact TB rates/contention from mech-spec; server truth (no heavy pred); text-first + WebRTC stretch; P[0]=100 norm + generic; in-mem single-proc. Update open Qs/STATUS on resolutions. Paste full design pkg + STATUS subs excerpts + anti-notes + task for handoff. High value: Tier 1 early prototypes + LW controller from arch for 007 + psych exs for 008 UX.

**Next for Agents**: Review arch + this + MANIFEST. Start 001/006 + 002 (parallel). Use full context. See STATUS for transcripts/subs.

---

**End of sub output. Full structured + suggested ready for append/handoff. Evidence from reads/greps across all targeted + scratch. All hold per verifs.**